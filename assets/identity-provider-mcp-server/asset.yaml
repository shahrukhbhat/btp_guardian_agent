apiVersion: asset.sap/v1
kind: Asset
type: mcp-server

metadata:
  name: identity-provider-mcp-server
  ordId: sap.btp:apiResource:identity-provider-mcp:v1
  version: 1.0.0
  description: MCP server wrapping the SAP BTP Identity Provider Management API (Trust Configuration) for CRUD of trusted identity providers
  translation: mcp-translation/translation.json
